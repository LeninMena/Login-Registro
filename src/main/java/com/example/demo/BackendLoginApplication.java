package com.example.demo;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

//Configuración inicial de la aplicación Spring Boot. Define el punto de entrada principal para ejecutar la aplicación.
@SpringBootApplication
public class BackendLoginApplication {

    public static void main(String[] args) {
        SpringApplication.run(BackendLoginApplication.class, args);
    }
}
